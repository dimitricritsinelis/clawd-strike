**ENGINEERING: CHANGES REQUIRED**

Reviewed **BZ-04 / R2.1-coordinated-details**, package SHA256:
`479000377710ad966a671c7315a9120c6ed3b39dfd3b4bdde44382f707d93a57`

All **195 controlled files match**, including the final recheck. No frozen-package changes detected.

**Blocking finding: roof authoring and placement transforms conflict.**

[roof-coordination.json:12657](/Users/dimitri2/Desktop/clawdstrike/docs/map-design/construction/roof-coordination.json:12657) specifies glTF coordinates `(eastOffset, height, northOffset)` with `baseCentrePlacement.yawDeg: 0`. [roofs.py:413](/Users/dimitri2/Desktop/clawdstrike/docs/map-design/construction/roofs.py:413) generates that combination for all 19 bundles. However, [buildAuthoredPlacements](/Users/dimitri2/Desktop/clawdstrike/apps/client/src/runtime/map/buildFacadeModels.ts:117) applies [the existing yaw conversion](/Users/dimitri2/Desktop/clawdstrike/apps/client/src/runtime/map/coordinateTransforms.ts:23), adding **180°**.

An in-memory fixture through the actual loader confirms:

- Caravan `ROOF_CELL_004` intended center: **(15.3, 31, 5.02) m**.
- Scheduled placement produces: **(-0.3, 46.5, 5.02) m**.
- Using `yawDeg: 180` restores the intended coordinates within floating-point precision.

This rotates roof cells away from their prescribed receivers and retirement coverage. Symmetric bundle bounding boxes cannot detect it.

**Required remedy:** reconcile the controlled authoring/placement recipe before handoff. With the existing loader and stated export frame, generate **180° placement yaw** for these bundles, update the schedules and integration explanation, and add an asymmetric coordinate regression check through the runtime transform. Do not change the shared legacy yaw helper indiscriminately.

**Recheck evidence:** prior stale roof references, arched-display clearance and graded-base conflicts are resolved. Both baseline spec hashes and the fresh runtime-derived survey match. Document/building validators, eight negative fixtures, roof/camera reproducibility and **176 in-memory generated-document comparisons** pass. All 25 zone elevation images, relevant plans/sections and both required reference images were inspected. **114 texture files** match recorded provenance hashes; the rug hash also matches.

Implementation-stage conditions remain the scheduled exported-triangle/interface checks, metadata propagation, shader/shadow behavior, roof batching, producer retirement, collider identity and measured performance. Actual-game appearance and user art acceptance remain separate.

No source edits, asset regeneration or game launch performed.

